version: '3'
services:
  dev:
    build: 
      context: .
      dockerfile: Dockerfile.nlp_app 
    volumes:
      - .:/app
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    
